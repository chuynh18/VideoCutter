<UserControl x:Class="VideoCutter.About"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:VideoCutter"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Margin="10,10,10,10">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock TextWrapping="Wrap">
            VideoCutter is a very basic front-end for FFMpeg to help you cut videos without any re-encoding.
            <LineBreak /><LineBreak />
            It requires the FFMpeg executable to function.  FFMpeg binaries for Windows can be downloaded from <Hyperlink NavigateUri="https://ffmpeg.zeranoe.com/builds/" Click="Uri_Click">ffmpeg.zeranoe.com</Hyperlink>.
            <LineBreak /><LineBreak />
            Unzip FFMpeg to a convenient location, then select the FFMpeg directory in the <Span Foreground="#FF0066CC" Cursor="Hand" MouseLeftButtonUp="Settings_Text_Clicked"><Underline>Settings tab</Underline></Span>.
        </TextBlock>

        
    </Grid>
</UserControl>
